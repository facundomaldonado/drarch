-Mapear un archivo .flabot a hechos logicos.

-Seleccionar el .flabot en el que se va a reconstruir la arquitectura.
  * Desde el flabto, agregar en el menu la opcion "Mapear al drarch".

-DepotRules folder (todos los .rub mas la declaracion de los facts)

-Definir el modelo de fases.
  * Agregar fases. (seleccionar el working set) click en el proyecto en "add fase".
  * Cunado se ejecuta una fase (click en .properties) se seleccionan las reglas de los .rub propios de la fase.
  * Crear una carpeta por fase (fase#).
  * Mensaje inidicando el # de fase que se esta ejecutando.
 
- Filtro para decidir en que .rub se deben colocar los nuevos hechos. 
 
-Achicar el scope del log.

-Agregar informacion del log para identificar paths por eventos.

-Casos de uso (Drarch y G2)

